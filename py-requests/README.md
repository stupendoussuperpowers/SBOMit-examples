### Building
`pip install .`

### Attestation Generation

`witness run -o test-att.json --step build -- pip install .`

#### With network-trace support

`witness run -a network-trace -o test-att.json --step build -- pip install .`

